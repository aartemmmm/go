# 06 — printf-drill

Сделайте вывод ровно как в примере, применив подходящие вербы `Printf`.
Ожидаемый вывод:
```
n=7 type=int hex=7
pi=3.14 quoted="go"
```

Подсказка: `%v`, `%T`, `%q`, для hex можно использовать `%x` с целыми.
